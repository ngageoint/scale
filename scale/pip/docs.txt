# This file lists all Python libraries required to build the docs on Linux
# This also requires the production.txt requirements
# Use command: pip install -r docs.txt

# Doc build packages
mock>=2.0.0,<2.1.0
semver
Sphinx>=1.5.3,<1.6.0
sphinx_rtd_theme>=0.1.9,<1
